# DoneBubble v0.1.1

首个 Windows x64 版本，记录“今天已经处理了多少事”。

- 常驻置顶气泡，拖动定位；点击「轻 / 中 / 重」即可记录，备注可选。
- 轻 / 中 / 重分别计 1 / 2 / 3 分；本地 SQLite 保存，今日历史支持删除。
- 低饱和度气泡颜色随每 5 分循环，并体现全天累计负载。
- 每满 5 分提醒休息，独立大字浮层倒计时 10 分钟，支持每次加减 5 分钟。
- 系统托盘、可选开机启动、位置及倒计时恢复。
- 不联网、不登录、无遥测。

## 使用

下载 `DoneBubble.exe`，放到固定目录后双击运行，无需安装 .NET。
用户数据位于 `%LOCALAPPDATA%\DoneBubble\`，升级时保留该目录即可。
`SHA256SUMS.txt` 提供可执行文件的 SHA-256 校验值。

## 验证及限制

已通过 Windows 发布包的 SQLite、旧数据库升级、计分、提醒、倒计时和 WPF 窗口冒烟测试。
当前仅支持 Windows 10 / 11 x64；未签名，无安装器或自动更新。
中文输入法、多显示器缩放及真实登录自启仍需在目标设备验收。
